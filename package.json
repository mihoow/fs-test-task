{
  "name": "fs-test-task",
  "private": true,
  "version": "1.0.0",
  "description": "Full Stack Test Task",
  "main": "index.js",
  "scripts": {
    "build:shared": "yarn workspace @app/shared build",
    "dev:fe": "yarn build:shared && cd fe && npm run dev",
    "dev:be": "yarn build:shared && concurrently \"docker compose watch\" \"docker compose logs -f backend\""
  },
  "author": "Illia S",
  "license": "ISC",
  "workspaces": [
    "fe",
    "be",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
